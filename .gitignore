__pycache__/
*.pyc
.venv/
venv/
.env
.env.local

# Interaction logs (raw prompt/response capture — may contain PII)
logs/

# Claude Code local state
.claude/

# Eval run outputs (regenerated by eval/run_eval.py)
eval/results.json
eval/results_summary.md

# Internal working note (candid gap analysis — commit deliberately if wanted)
audit.txt

# Node / Vite build output (also ignored inside nalam/)
node_modules/
dist/
dist.zip

# OS / editor
.DS_Store
.vscode/
.idea/

# Added by agent
knowledge_base/embeddings/
nalam/logs/
*.log
