{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd nalam && npm install && npm run build",
  "outputDirectory": "nalam/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
